package tests.junit;

/*public class LoginTest {
    WebDriver driver;
    @Test
    String loginPageUrl = "https://www.saucedemo.com/v1/index.html";
    public  void successfullogin() {
        By usernameTextArea = null;
        By passwordTextArea = null;
        By loginButton = null;
        driver.findElement(usernameTextArea).sendKeys("");
        driver.findElement(passwordTextArea).sendKeys("");
        driver.findElement(loginButton).click();
        var currentUrl

    }
    public  void LockedOutErrormsg() {}
    public  void lockedOutNotRedirecttOlandingPage() {}


        System.setProperty("webdriver.chrome.driver", "path/to/chromedriver.exe");

    WebDriver driver = new ChromeDriver();

    driver.navigate().to("https://www.saucedemo.com/v1/index.html");

    WebElement usernameElement = driver.findElement(By.id("user-name"));

    assert usernameElement.isDisplayed() : "Username field is not displayed";

    WebElement passwordElement = driver.findElement(By.id("password"));

    assert passwordElement.isEnabled() : "Password field is not enabled";

    driver.quit();




}
}*/
